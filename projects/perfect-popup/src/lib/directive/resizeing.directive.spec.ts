import { ResizeingDirective } from './resizeing.directive';

describe('ResizeingDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeingDirective();
    expect(directive).toBeTruthy();
  });
});
